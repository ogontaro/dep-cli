{
  "name": "dep",
  "version": "0.3.0",
  "private": true,
  "license": "MIT",
  "type": "module",
  "bin": {
    "dep": "./src/cli.ts"
  },
  "scripts": {
    "build": "bun build ./src/cli.ts --compile --outfile dist/dep",
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "yaml": "^2.5.1"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.6.0"
  }
}
