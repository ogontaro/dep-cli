{
  "name": "depctl",
  "version": "0.1.1",
  "private": true,
  "license": "MIT",
  "type": "module",
  "bin": {
    "depctl": "./src/cli.ts"
  },
  "scripts": {
    "build": "bun build ./src/cli.ts --compile --outfile dist/depctl",
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "yaml": "^2.5.1"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.6.0"
  }
}
